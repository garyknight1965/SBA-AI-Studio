[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sba-ai-studio"
version = "2.1.0"
description = "AI Assisted DaVinci Resolve Workflow"
authors = [
    {name = "Gary Knight"}
]
requires-python = ">=3.10"
